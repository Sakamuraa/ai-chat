// language: TypeScript, file: app/not-found.tsx, target: Next.js (client karena Phosphor + i18n)
"use client";

import NotFoundView from "@/components/not-found-view";

export default function NotFound() {
  return <NotFoundView />;
}
